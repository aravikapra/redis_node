import { RedisArgument, NumberReply } from '../RESP/types';
import { ZKeys } from './generic-transformers';
export interface ZUnionOptions {
    AGGREGATE?: 'SUM' | 'MIN' | 'MAX';
}
declare const _default: {
    readonly FIRST_KEY_INDEX: 1;
    readonly IS_READ_ONLY: false;
    readonly transformArguments: (this: void, destination: RedisArgument, keys: ZKeys, options?: ZUnionOptions) => import("../RESP/types").CommandArguments;
    readonly transformReply: () => NumberReply;
};
export default _default;
