"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const generic_transformers_1 = require("./generic-transformers");
exports.default = {
    FIRST_KEY_INDEX: 2,
    IS_READ_ONLY: true,
    transformArguments(keys, options // `number` for backwards compatibility
    ) {
        const args = (0, generic_transformers_1.pushVariadicArgument)(['SINTERCARD'], keys);
        if (typeof options === 'number') { // backwards compatibility
            args.push('LIMIT', options.toString());
        }
        else if (options?.LIMIT !== undefined) {
            args.push('LIMIT', options.LIMIT.toString());
        }
        return args;
    },
    transformReply: undefined
};
