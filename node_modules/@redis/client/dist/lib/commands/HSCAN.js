"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const SCAN_1 = require("./SCAN");
exports.default = {
    FIRST_KEY_INDEX: 1,
    IS_READ_ONLY: true,
    transformArguments(key, cursor, options) {
        return (0, SCAN_1.pushScanArguments)(['HSCAN', key], cursor, options);
    },
    transformReply([cursor, rawEntries]) {
        const entries = [];
        let i = 0;
        while (i < rawEntries.length) {
            entries.push({
                field: rawEntries[i++],
                value: rawEntries[i++]
            });
        }
        return {
            cursor,
            entries
        };
    }
};
