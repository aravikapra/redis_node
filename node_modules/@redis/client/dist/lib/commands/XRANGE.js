"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.transformXRangeArguments = void 0;
const generic_transformers_1 = require("./generic-transformers");
function transformXRangeArguments(command, key, start, end, options) {
    const args = [command, key, start, end];
    if (options?.COUNT) {
        args.push('COUNT', options.COUNT.toString());
    }
    return args;
}
exports.transformXRangeArguments = transformXRangeArguments;
exports.default = {
    FIRST_KEY_INDEX: 1,
    IS_READ_ONLY: true,
    transformArguments: transformXRangeArguments.bind(undefined, 'XRANGE'),
    transformReply(reply) {
        return reply.map(generic_transformers_1.transformStreamMessageReply);
    }
};
